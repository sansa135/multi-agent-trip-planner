# Leave USE_REAL_LLM unset or "false" to run entirely offline with the
# deterministic MockLLMClient (no API key or cost needed).
#
# Set USE_REAL_LLM=true and provide your API key to have every agent call
# a real Claude model instead of the mock.

USE_REAL_LLM=false
ANTHROPIC_API_KEY=
